# Synatx -> np.isnan(array)

import numpy as np

# -> isnan Function Detech the nan so You want first write the np.nan

arr = np.array([1, 2, 3, np.nan, 4, np.nan])

print(np.isnan(arr))

# -> isnan Function genrally returns in True 
